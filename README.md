# Запустить бэк
Перед запуском настраиваем файл ```.env```
```# .env
OPENAI_API_KEY =
BASE_URL =
YASHA_POGODA_KEY =
```
Далее запускаем

```docker compose up .```

Теперь наш бэк доступен по адресу

```http://localhost:5000```
